# GSD Application Environment Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# =============================================================================
# Application URL
# =============================================================================
# Full HTTPS URL (managed by external reverse proxy)
APP_URL=https://getsd.bieda.it

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL password for user 'gsd_user'
# Generate with: openssl rand -base64 24
DB_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# =============================================================================
# JWT Authentication
# =============================================================================
# Secret key for signing JWT tokens
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_SECURE_SECRET

# JWT token expiration time (e.g., 7d, 30d, 1h)
JWT_EXPIRES_IN=7d

# =============================================================================
# Google OAuth Configuration
# =============================================================================
# Get these from: https://console.cloud.google.com/apis/credentials
#
# Important: Add authorized redirect URI:
#   https://getsd.bieda.it/api/auth/google/callback
#
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# Notes
# =============================================================================
#
# 1. Generate secure passwords:
#    DB_PASSWORD:  openssl rand -base64 24
#    JWT_SECRET:   openssl rand -base64 32
#
# 2. Google OAuth Setup:
#    - Go to https://console.cloud.google.com
#    - Create OAuth 2.0 credentials
#    - Add redirect URI: https://getsd.bieda.it/api/auth/google/callback
#
# 3. Port Configuration:
#    - Application is exposed on port 8080 (configured in docker-compose.yml)
#    - External reverse proxy should forward to: your-server-ip:8080
#
# 4. Security:
#    - Keep this file secure (chmod 600 .env)
#    - Never commit .env to git
#    - Back up credentials securely
#
